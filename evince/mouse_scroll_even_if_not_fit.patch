commit 7be3fdc940c8fb54c5b74dedda2763a465442086
Author: Michael Fiedler <dev@michael-fiedler.net>
Date:   Thu Mar 13 11:21:06 2014 +0100

    Scroll through pages by mouse wheel even if not in best fit mode

diff --git a/libview/ev-view.c b/libview/ev-view.c
index 06017b5..a1cb5c6 100644
--- a/libview/ev-view.c
+++ b/libview/ev-view.c
@@ -3326,7 +3326,7 @@ ev_view_scroll_event (GtkWidget *widget, GdkEventScroll *event)
 		state &= ~GDK_SHIFT_MASK;
 	}
 
-	if (state == 0 && view->sizing_mode == EV_SIZING_BEST_FIT && !view->continuous) {
+	if (state == 0 && !view->continuous) {
 		switch (event->direction) {
 		        case GDK_SCROLL_DOWN:
 		        case GDK_SCROLL_RIGHT:
